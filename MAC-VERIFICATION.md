# Mac 1.8.2 검사 및 수정 — 2026-09-14

맥 버전의 기능과 보안 경계를 검사하고 다음 안정성 문제를 수정했습니다.

- 동네 검색 서비스가 오류를 반환하면 도시 검색으로 넘어가도록 수정했습니다. 검색 취소 시에는 대체 요청을 보내지 않습니다.
- 비정상적으로 큰 값이나 무한대·NaN 시간 값으로 타이머 표시가 종료될 수 있는 문제를 막았습니다.
- 이전 저장 데이터의 디자이너 팔레트가 비어 있거나 일부만 있으면 기본 색상으로 보완하고 기존 노트는 보존합니다.
- 잘못 저장된 시간 값에서 시간 선택 목록과 수면 시간 계산이 정수 오버플로로 종료될 수 있는 문제를 막았습니다.
- 기존 빌드 폴더와 병합할 때 남는 Finder 메타데이터·이전 리소스를 제거하고 최종 복사본도 서명 검사하도록 빌드 절차를 수정했습니다.
- 영어 날씨 제공자 안내에 실제 검색 서비스인 Photon/OpenStreetMap을 명시했습니다.

## 검증

로컬 macOS Swift 테스트 45개 중 44개 통과, 기존 1개 건너뜀, 실패 0개. 추가한 6개 테스트는 시간 값 복구, 팔레트 복원, 시간 계산, 날씨 검색 실패·취소·이중 실패를 검증합니다. 이 다운로드는 동일 커밋의 GitHub macOS 테스트, 앱 빌드 및 서명 무결성 검사가 통과한 뒤 공개됩니다.

Codex Security의 독립 소스 감사는 수정 전 맥 소스 22개 Swift 파일을 검토했고 입증된 보안 취약점을 보고하지 않았습니다. 고정 HTTPS API 주소, 키체인 저장, 리디렉션 차단, 요청별 사용자 확인, 외부 응답의 텍스트 표시를 검토했습니다. 이는 모든 취약점이 없다는 보증이 아닙니다. 저장 값 관련 수정은 보안 취약점이 아닌 안정성 결함입니다.

Sentry 인증과 ProductOS 프로젝트가 연결되지 않아 서비스 오류 로그는 확인하지 못했습니다. Context7으로 Apple SwiftUI 문서를 확인했습니다. Android 테스트 도구는 맥에 적용하지 않았습니다. 실제 유료 API 요청과 외부 서비스 장애 유발은 수행하지 않았습니다. 외부 검색 실패는 네트워크를 사용하지 않는 테스트로 검증했습니다.

## 설치

MoonRabbit-macOS.zip을 풀고 기존 앱을 종료한 뒤 새 MoonRabbit.app을 실행하세요. 기존 사용자 기록과 API 키는 삭제하지 않습니다. 앱은 로컬 ad-hoc 서명이며 Apple 공증을 받지 않았습니다. 이번 릴리스는 Mac 전용입니다.
