const {test}=require('node:test');const assert=require('node:assert/strict');const M=require('./core.js');
test('week is seven inclusive calendar days across month boundary',()=>{assert.equal(M.periodEnd('2026-09-28','week'),'2026-10-04')});
test('calendar month and leap year periods clamp before inclusive end',()=>{assert.equal(M.periodEnd('2026-01-31','month'),'2026-02-27');assert.equal(M.periodEnd('2024-02-29','year'),'2025-02-27');assert.equal(M.periodEnd('2026-01-01','100'),'2026-04-10')});
test('D-day handles past/future/today and custom prefix',()=>{assert.equal(M.ddayLabel('2026-10-01','2026-09-30'),'D−1');assert.equal(M.ddayLabel('2026-09-29','2026-09-30'),'D+1');assert.equal(M.ddayLabel('2026-09-30','2026-09-30','마감'),'마감 0')});
test('stored settings preserve names and safely normalize appearance',()=>{const s=M.normalize({title:'나의 플래너',accent:'url(https://example.com)',role:'bogus',lang:'en',size:999});assert.equal(s.title,'나의 플래너');assert.equal(s.accent,M.defaults().accent);assert.equal(s.role,'developer');assert.equal(s.size,280);assert.equal(s.lang,'en')});
test('task IDs survive JSON and separate entries do not collide',()=>{const a=M.task(),b=M.task();assert.notEqual(a.id,b.id);assert.equal(JSON.parse(JSON.stringify(a)).id,a.id)});
test('priority reminders fire once at each lead and include tomorrow across midnight',()=>{const s=M.defaults();s.role='developer';const task=M.task(30,30);task.title='Review';task.priority='high';s.days['2026-10-01:developer']={tasks:[task]};assert.equal(M.reminders(s,new Date(2026,8,30,23,30)).length,1);assert.equal(M.reminders(s,new Date(2026,8,30,23,30,10)).length,0);assert.equal(M.reminders(s,new Date(2026,9,1,0,0)).at(0).lead,30);assert.equal(M.reminders(s,new Date(2026,9,1,0,20)).at(0).lead,10);task.done=true;assert.equal(M.upcoming(s,new Date(2026,9,1,0,20)).length,0)});
test('upcoming ranks high priority first and ignores deleted tasks',()=>{const s=M.defaults(),a=M.task(600),b=M.task(610);a.title='Low';a.priority='low';b.title='High';b.priority='high';s.days['2026-09-30:developer']={tasks:[a,b]};assert.equal(M.upcoming(s,new Date(2026,8,30,9,0))[0].task.title,'High');s.days['2026-09-30:developer'].tasks=[];assert.equal(M.reminders(s,new Date(2026,8,30,9,0)).length,0)});
